# 编程智能体(coding agent)设计方案

## 1. 项目概述

本项目从零实现一个类 Claude Code 的**编程智能体(coding agent)**:与大语言模型对话,能自主地读写文件、执行命令、搜索代码,把用户交给它的编程任务拆解、执行并完成。

- **技术栈**:Python 3.13,`requests` + 手写 SSE 解析,`rich`(终端 UI),`FastAPI + uvicorn`(网页端)。
- **默认 LLM**:DeepSeek(OpenAI 兼容网关,`https://api.deepseek.com`);`base_url`/模型/API key 全部可配置。
- **核心约束**:不使用任何 agent 框架/SDK(LangChain、OpenAI Agents SDK、Claude Agent SDK、AutoGen 等);对话历史与上下文管理、工具定义与本地执行、模型输出解析、循环终止条件、错误处理等**重要逻辑全部自行实现**。
- **双端交互**:终端 CLI(流式 + 多轮 + 斜杠命令)与网页端(对话管理 + SSE 流式)共用同一套 agent 核心,仅 UI 层不同。

## 2. 总体架构

```
                 ┌────────────────────────────┐
                 │    UI 层(可插拔,同一 UISink) │
                 │  CLI(rich)  /  Web(FastAPI+SSE)
                 └──────────────┬─────────────┘
                                │ 事件(event/ask)
                 ┌──────────────▼─────────────┐
                 │   agent/loop.py   ReAct 循环 │
                 │  想 → 调工具 → 看结果 → 再决策  │
                 └───┬──────┬──────┬──────┬─────┘
        ┌────────────┘      │      │      └────────────┐
        ▼                   ▼      ▼                    ▼
┌──────────────┐  ┌───────────────┐  ┌──────────────┐  ┌──────────────┐
│  tools/ 工具集 │  │ agent/permissions│ │ llm/history   │  │ 扩展系统        │
│ 6核心+记忆+    │  │ 5 层纵深权限     │ │ 上下文管理与压缩 │  │ MCP/技能/钩子/  │
│ 子任务+MCP 工具 │  └───────────────┘  └──────────────┘  │ 斜杠命令/团队/   │
└──────────────┘                                          │ worktree/记忆   │
        │                                                └──────────────┘
        ▼
┌──────────────┐
│ llm/client.py │  自写 OpenAI 兼容客户端(requests + SSE + tool calling)
└──────────────┘
```

**数据流**:用户输入 → `History`(维护消息与 system 提示)→ `ChatClient` 请求模型 → 流式返回文本(实时渲染)+ 原生 tool calling → 逐条过 `PermissionPolicy` 裁决 → 本地执行工具 → 结果截断后写回 `History` → 继续循环;直到模型不再调用工具(输出最终答复)、达到最大迭代、用户中断或上下文预算耗尽。

## 3. 核心设计(自写部分)

### 3.1 自写 OpenAI 兼容客户端(`llm/client.py`)

不依赖 `openai` 包,用 `requests` 直连任意 OpenAI 兼容网关:

- **手写 SSE 解析**:逐行识别 `data:` 与 `[DONE]`,忽略心跳注释行,`json.loads` 容错。
- **流式 tool calling 累积**:按 `index` 把分片到达的 `tool_calls` 的 `id/name/arguments` 增量拼接,结束时聚合成结构化 `ToolCall`。
- **错误处理**:非 2xx 时对 408/429/5xx 指数退避重试;若网关不支持 `stream_options.include_usage`,遇 400 自动去掉重试一次;401/403 直接报鉴权失败;JSON 解析失败、空响应均有兜底。
- **流式与非流式两用**:`chat_stream()` 供主循环边收边吐,`chat()` 供摘要/团队汇总等内部调用。

### 3.2 对话历史与上下文管理(`llm/history.py` + `tokens.py`)

- 消息采用 OpenAI Chat Completions 结构(`system/user/assistant/tool` + `tool_calls`/`tool_call_id`),直接兼容模型原生 tool calling。
- **system 提示由多段可插拔内容动态拼装**:基础提示、跨会话记忆、已装载技能、压缩摘要。
- **超长工具输出自动截断**:保留头尾、中间省略,避免 token 爆炸。
- **上下文压缩**:超预算时把旧消息交给 LLM 摘要成一段 system 上下文,保留最近 12 条原文不压缩;摘要失败时**回滚、宁可多用 token 也不丢上下文**。
- **token 估算**:零依赖启发式(中文 ≈ 1 token/字符,ASCII ≈ 1 token/4 字符),用于预算触发与 `/cost` 统计。

### 3.3 工具定义与本地执行(`tools/`)

统一 `Tool` 接口:`name/description/parameters(JSON-schema)/run(ctx, **kwargs)`,模型以原生 function calling 方式调用,参数经 JSON-schema 定义。**工具均在本机进程内执行**(WebSearch 为联网搜索,需网络):

| 工具 | 说明 |
|---|---|
| `ReadFile` | 带行号/行区间读取,二进制与目录检测 |
| `WriteFile` | 覆盖写/新建,自动建父目录 |
| `EditFile` | **严格旧文本匹配**的差异编辑:old_string 必须逐字唯一命中,否则报错不猜测 |
| `Bash` | `subprocess` 执行,超时强杀进程树,输出截断 |
| `Glob` | 文件名模式搜索(纯 Python,`pathlib.glob`) |
| `Grep` | 内容正则搜索(纯 Python,支持上下文行/忽略大小写/字面量) |
| `WebSearch` | 联网搜索(无 key 抓取 Bing),返回标题/链接/摘要 |
| `MemoryRecall / MemorySave` | 跨会话记忆读写 |
| `DispatchTask` | 子任务分发(并行子 Agent) |
| MCP 远端工具 | 经 `MCPToolAdapter` 注册成本地工具,Agent 无感调用 |

工具执行有统一返回值 `ToolResult(success/output/error)`,单工具失败只记为一条工具结果,**不中断整个任务**。

### 3.4 ReAct 主循环(`agent/loop.py`)

```
循环(每轮):
  1. 预算检查:超预算则压缩历史;
  2. 调模型(流式)→ 累积文本与 tool_calls;
  3. 无 tool_calls → 输出最终答复,循环终止;
  4. 有 tool_calls → 记录 assistant 消息,逐条:权限裁决 → 钩子 veto 检查 → 执行 → 结果写回;
  5. 回到 1,直至终止条件。
终止条件:无工具调用 / 达到最大迭代(max_iterations) / 用户中断(Ctrl+C) / 上下文预算耗尽。
```

- 事件经 `UISink` 接口派发,CLI 与 Web 各自实现渲染,共用同一套循环。
- 单轮 LLM 失败:重试后仍失败则记录错误终止;单工具异常捕获,不影响后续。

### 3.5 5 层纵深权限防御(`agent/permissions.py`)

| 层 | 规则 | 行为 |
|---|---|---|
| 1 | 工具级规则 `deny/allow/ask_tools` | 按名单裁决 |
| 2 | 敏感路径拦截(`.git`、`.env`、`*.key`、`credentials` 等) | 直接拒绝 |
| 3 | 工作区沙箱:路径越出工作区 | 需确认/拒绝 |
| 4 | 危险命令正则(`rm -rf`、`git push --force`、`format C:` 等) | 需确认 |
| 5 | 兜底:白名单命令放行;其余按 `mode`(interactive / auto-approve / deny) | 逐次问 / 放行 / 拒绝 |

CLI 用 rich 交互确认,Web 端由策略 + 模式裁决(无交互时默认拒绝),无 API key 时不发起任何请求。

## 4. 扩展系统

- **MCP 协议**(`tools/mcp.py`):自写 stdio + JSON-RPC 2.0 客户端,实现 `initialize / initialized / tools/list / tools/call`,把任意符合规范的本地工具服务挂载为工具;附演示 server(`examples/mcp_server_demo.py`)。
- **Skill 技能包**(`skills/`):`SKILL.md`(frontmatter + instructions)+ `tools/*.py`(可选额外工具);装载后 instructions 注入 system、工具注册进注册表。内置示例:code-review(代码审查)、tsp(TSP 流程)、scrum(Scrum 迭代)。
- **Slash 命令**(`commands/`):内置 18 条(help/status/exit/clear/model/tools/permissions/memory/skills/compact/cost/mcp/team/worktree/resume/plan/execute/checkpoint)+ 自定义 `.coding_agent/commands/*.md`(`{args}` 模板)。示例:plan。
- **Hook 生命周期**(`hooks/manager.py`):`session/agent start/end`、`pre/post_tool_call`、`pre/post_message`;支持 shell 命令或 Python 可调用对象,`pre_tool_call` 可 veto;失败只记录不致命。
- **跨会话记忆**(`agent/memory.py`):项目级 `.coding_agent/memory/project.md` + 用户级 `~/.coding_agent/memory/user.md`,会话开始注入 system,Agent 可用工具持续写入。
- **SubAgent**(`agent/subagent.py`):复用同一 `AgentLoop`,独立历史/可裁剪工具,线程池并行。
- **Git Worktree**(`agent/worktree.py`):`git worktree add/list/remove` 封装,并行隔离。
- **Agent Teams**(`agent/teams.py`):多名角色化成员(独立子 Agent)并行作业,负责人汇总;配置在 `config.yaml` 的 `teams`。内置示例:review-team(架构师/工程师/审查员)、tsp-team(TSP 六角色)、scrum-team(产品负责人/Scrum教练/开发/测试)。

## 5. 双端 UI

- **CLI**(`ui/cli.py`):rich 流式文本、工具调用面板(参数/结果/状态着色)、多行输入(尾随 `\` 续行)、Ctrl+C 中断当前回合、斜杠命令。三种运行方式:`python -m codingagent`(交互)、`run "任务"`(一次性)、`web`(网页端)。
- **Web**(`ui/web.py`):FastAPI + SSE。`/api/config`(模型/工具/技能,不含密钥)、会话 CRUD、`POST /api/chat` 流式返回 `text/tool_call/tool_result/status/done/error` 事件;单文件前端(无构建),会话侧栏 + 聊天窗 + 流式渲染 + 斜杠命令;工具栏可即时切换整体配色主题(深色/浅色/暖色/夜间蓝/南大紫/软件蓝),选择记忆在浏览器本地。
- 两者共用 `UISink` 事件通道:同一事件源,两种渲染。

## 6. 配置与安全

- 配置优先级:默认值 < `config.yaml` < 用户级/项目级覆盖 < `.env` 文件 < 进程环境变量 < 命令行参数。
- **API key 一律通过环境变量或 `.env` 读取**(`config.py::get_secret`),代码与仓库内无任何真实密钥;`.env` 已 gitignore;`/api/config` 与 `doctor` 只返回"是否已配置",不回显 key。
- 模型、base_url、温度、上下文预算、权限策略、MCP servers、teams 等均在 `config.yaml` 可配;运行中可用 `/model` 动态切换模型。

## 7. 目录结构

```
coding_agent/
├── codingagent/            # 主包
│   ├── __main__.py         # 入口:CLI / run / web / doctor
│   ├── config.py           # 配置加载(含自写 .env 解析)
│   ├── session.py          # 会话装配(CLI/Web 共用)
│   ├── types.py            # 共享数据类型(ToolCall/Result/Usage)
│   ├── prompts.py          # 基础 system 提示
│   ├── llm/                # client.py / history.py / tokens.py
│   ├── tools/              # base/registry/files/shell/search/websearch/memory_tools/mcp/subagent_tool
│   ├── agent/              # loop/permissions/memory/subagent/worktree/teams
│   ├── skills/             # 技能扫描与装载
│   ├── hooks/              # 生命周期钩子
│   ├── commands/           # 斜杠命令(slash + builtins)
│   └── ui/                 # cli.py / web.py
├── skills/code-review/     # 示例技能包(代码审查)
├── skills/tsp/             # 示例技能包(TSP 流程)
├── skills/scrum/           # 示例技能包(Scrum 迭代)
├── examples/               # 示例 MCP server、自定义命令
├── config.yaml             # 默认配置
├── .env.example            # 凭据模板
├── requirements.txt        # requests / rich / fastapi / uvicorn / PyYAML
├── tests/                  # 148 个 pytest(核心逻辑,mock LLM,免真实 API)
└── README / 项目结构 / 项目使用方式说明 / 设计方案
```

## 8. 验证方式

1. **环境**:conda 创建 `coding_agent` 环境(Python 3.13),`pip install -r requirements.txt`。
2. **单元/集成测试**:`pytest tests/` —— 148 个用例覆盖配置/.env/token、10 大工具(含 WebSearch)、EditFile 严格匹配、权限决策矩阵、ReAct 循环端到端(mock LLM)、usage 跨请求累计、上下文压缩、斜杠命令、钩子、MCP 对假 server 的往返、技能、检查点/回滚、Web API(CRUD + SSE)。**免真实 API key**。
3. **自检**:`python -m codingagent doctor` 输出工具/命令/技能/MCP 状态。
4. **实网验证**:配置 `DEEPSEEK_API_KEY` 后,CLI 与 Web 各跑一次真实任务(如"读 README 后改一处文本")。