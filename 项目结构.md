# 项目结构

## 目录树

```
coding_agent/                              # 仓库根(工作区)
├── codingagent/                           # 主包
│   ├── __init__.py                        # 版本号
│   ├── __main__.py                        # 命令行入口:交互 / run / web / doctor
│   ├── config.py                          # 配置系统:默认值+config.yaml+.env+env 合并
│   ├── session.py                         # Session:装配 config/client/工具/记忆/技能/命令
│   ├── types.py                           # 共享类型:ToolCall / ToolResult / Usage / FinalResult
│   ├── prompts.py                         # 基础 system 提示
│   ├── llm/
│   │   ├── client.py                      # 自写 OpenAI 兼容客户端(requests+SSE+tool calling)
│   │   ├── history.py                     # 对话历史与上下文管理、超预算压缩
│   │   └── tokens.py                      # 启发式 token 估算
│   ├── tools/
│   │   ├── base.py                        # Tool 基类 + ToolContext(参数 JSON-schema、路径解析)
│   │   ├── registry.py                    # 工具注册表(大小写不敏感查找、schema 导出)
│   │   ├── files.py                       # ReadFile / WriteFile / EditFile(严格差异编辑)
│   │   ├── shell.py                       # Bash(沙箱执行、超时、截断)
│   │   ├── search.py                      # Glob / Grep(纯 Python 实现)
│   │   ├── memory_tools.py                # MemoryRecall / MemorySave
│   │   ├── subagent_tool.py               # DispatchTask(子任务并行分发)
│   │   └── mcp.py                         # MCP 客户端(stdio+JSON-RPC 2.0)+ 工具适配
│   ├── agent/
│   │   ├── loop.py                        # ReAct 主循环 + UISink 事件接口
│   │   ├── permissions.py                 # 5 层纵深权限防御
│   │   ├── memory.py                      # 跨会话记忆(项目级+用户级)
│   │   ├── subagent.py                    # 独立上下文子 Agent、线程池并行
│   │   ├── worktree.py                    # Git worktree 封装
│   │   └── teams.py                       # 多 Agent 团队编排
│   ├── skills/
│   │   └── manager.py                     # 技能扫描/装载/卸载(SKILL.md + tools/*.py)
│   ├── hooks/
│   │   └── manager.py                     # 生命周期钩子(shell 命令 / Python 可调用)
│   ├── commands/
│   │   ├── slash.py                       # 斜杠命令框架 + 自定义 md 装载
│   │   └── builtins.py                    # 15 条内置命令
│   └── ui/
│       ├── cli.py                         # 终端 UI(rich 流式/多行/面板)+ 一次性模式
│       └── web.py                         # FastAPI + SSE + 单文件前端
├── skills/
│   └── code-review/                       # 示例技能包(SKILL.md + tools/report_status.py)
├── examples/
│   ├── mcp_server_demo.py                 # 演示 MCP server(stdio,echo/add 两个工具)
│   └── commands/plan.md                   # 示例自定义斜杠命令(/plan)
├── tests/                                 # 42 个 pytest(conftest 含 mock LLM)
├── config.yaml                            # 默认配置(模型/权限/MCP/teams)
├── .env.example                           # 凭据模板(复制为 .env 使用)
├── .gitignore                             # 忽略 .env、.coding_agent/ 运行时数据等
├── requirements.txt                       # requests / rich / fastapi / uvicorn / PyYAML
├── README.md                              # 项目简介
├── README.txt                             # 提交物(仓库地址、运行方式、特色)
├── 设计方案.md                             # 设计文档
└── 项目结构.md                             # 本文档
```

## 各模块职责与关键类/函数

### 入口层

| 位置 | 职责 |
|---|---|
| `__main__.py::main` | 解析子命令:`run`(一次性)/ `web` / `doctor` / 缺省交互 REPL;Windows 下统一 stdout 为 UTF-8 |
| `session.py::Session` | 组装 config/client/registry/memory/hooks/skills/mcp/slash,提供 `make_agent()` 工厂与会话历史持久化(load/save) |

### LLM 层

| 文件 | 关键类/函数 | 职责 |
|---|---|---|
| `llm/client.py` | `ChatClient.chat_stream()` | 流式请求,边收边吐文本 delta,聚合 tool_calls |
| | `ChatClient._iter_sse()` | 手写 SSE 解析(`data:`/`[DONE]`/心跳) |
| | `ChatClient._do_request()` | 重试(指数退避)、400 回退、鉴权错误 |
| | `client_from_config()` | 由 Config 构造客户端 |
| `llm/history.py` | `History` | 消息列表、可插拔 system 段、`to_api()`、`should_compact()`、`compact()` |
| `llm/tokens.py` | `estimate_tokens()` | 中文≈1 token/字符、ASCII≈1/4 |

### 工具层

| 工具 | 文件 | 说明 |
|---|---|---|
| `ReadFile` | `tools/files.py` | 行号/行区间读取,目录、二进制检测 |
| `WriteFile` | `tools/files.py` | 覆盖/新建,自动建父目录 |
| `EditFile` | `tools/files.py` | **严格旧文本匹配**:old_string 必须逐字唯一命中,一次一处 |
| `Bash` | `tools/shell.py` | 优先 `bash -c`,超时强杀进程树,输出截断 |
| `Glob` | `tools/search.py` | 文件名模式,`**` 递归,跳过 `.git`/`node_modules` |
| `Grep` | `tools/search.py` | 正则/字面量,上下文行,`file:line:content` 输出 |
| `MemoryRecall` | `tools/memory_tools.py` | 读记忆(all/project/user) |
| `MemorySave` | `tools/memory_tools.py` | 写一条带时间戳的记忆 |
| `DispatchTask` | `tools/subagent_tool.py` | 单/多子任务并行分发,独立上下文 |
| MCP 工具 | `tools/mcp.py::MCPToolAdapter` | 远端工具包装成本地 Tool |

`tools/base.py::ToolContext` 向工具注入工作区/权限/记忆/LLM 引用与 `ask`/`emit` 回调,保证工具与 UI 解耦。

### Agent 层

| 文件 | 关键类 | 职责 |
|---|---|---|
| `agent/loop.py` | `AgentLoop.run()` | ReAct 主循环;`UISink` 是 CLI/Web 共用的事件接口 |
| `agent/permissions.py` | `PermissionPolicy.decide()` | 5 层裁决,返回 ALLOW/ASK/DENY |
| `agent/memory.py` | `MemoryStore` | 项目 + 用户记忆读写 |
| `agent/subagent.py` | `SubAgent` / `run_subagents_parallel()` | 独立上下文子 Agent,线程池并行 |
| `agent/worktree.py` | `WorktreeManager` | `git worktree` add/list/remove |
| `agent/teams.py` | `Team.run()` | 成员并行 → 负责人汇总 |

### 扩展层

| 文件 | 关键类 | 职责 |
|---|---|---|
| `skills/manager.py` | `SkillManager` | 扫描 `skills/` 等目录,`load/unload`,工具注册 |
| `hooks/manager.py` | `HookManager` | 事件分发,`fire_pre_tool()` 可 veto |
| `commands/slash.py` | `SlashRegistry` | 注册/匹配/执行,自定义 md 装载 |
| `commands/builtins.py` | `register_builtin_commands()` | 15 条内置命令 |
| `tools/mcp.py` | `MCPClient` / `MCPManager` | stdio + JSON-RPC 2.0 客户端,连接管理 |

### UI 层

| 文件 | 关键类/函数 | 职责 |
|---|---|---|
| `ui/cli.py` | `CLIUI` | 流式文本、工具面板、状态行、`ask()` 交互确认 |
| | `run_cli()` | 交互 REPL(多行输入、斜杠命令、Ctrl+C) |
| | `run_once()` | 一次性模式(视频/脚本演示) |
| `ui/web.py` | `create_app()` | FastAPI 应用工厂 |
| | `ConversationStore` | 会话 CRUD 与历史持久化 |
| | `SSEUI` | 把 agent 事件桥接到 asyncio 队列(线程安全) |
| | `_INDEX_HTML` | 单文件前端(无构建) |

## 数据流

```
用户输入(CLI 多行 / Web 聊天框 / 斜杠命令)
   │
   ▼
Session.make_agent() → AgentLoop
   │
   ▼  AgentLoop.run(text)
   │  History.append(user)
   │  while 未终止:
   │    ├─ 预算检查 → 超限则 History.compact(LLM 摘要)
   │    ├─ client.chat_stream(history.to_api(), schemas)
   │    │    └─ 流式文本 → UI.event("text") 实时渲染
   │    │    └─ tool_calls → 累积为 [ToolCall]
   │    ├─ 无 tool_calls → 输出最终答复 → 终止
   │    └─ 逐条 tool_calls:
   │         ├─ PermissionPolicy.decide() → DENY/ASK/ALLOW
   │         ├─ 钩子 fire_pre_tool()(可 veto)
   │         ├─ tool.run(ctx, **args)  ← 本地执行
   │         ├─ UI.event("tool_result")
   │         └─ History.append(tool 消息,超长截断)
   │
   ▼
FinalResult(text, success, iterations, usage, elapsed)
   │
   ├─ CLI: 打印最终结果
   └─ Web: SSE 发送 done 事件,历史持久化到 .coding_agent/sessions/
```
