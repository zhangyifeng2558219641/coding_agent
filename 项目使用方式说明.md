# 项目使用方式说明

## 一、环境准备

本项目使用 **conda** 管理运行环境,环境名为 `coding_agent`(Python 3.13)。

### 1. 创建 conda 环境并安装依赖

```bash
# 创建环境(已创建过可跳过)
conda create -n coding_agent python=3.13 -y

# 激活环境
conda activate coding_agent

# 安装依赖
pip install -r requirements.txt

# 把本包可编辑安装进环境 —— 之后可在任意目录启动,用本 agent 处理其它项目
pip install -e .
```

`requirements.txt` 内容:

```
requests>=2.31.0
rich>=13.7.0
fastapi>=0.110.0
uvicorn>=0.29.0
PyYAML>=6.0
```

> 提示:
> - 若 `conda activate` 不可用,可用 `conda run -n coding_agent python ...` 直接调用。
> - Windows 下直接使用环境内的解释器路径亦可:`C:\Users\<你>\anaconda3\envs\coding_agent\python.exe`。

### 2. 配置 API key(模型凭据)

**所有 API key 一律通过环境变量或 `.env` 提供,绝不写入代码或仓库。**

复制模板并填写真实 key:

```bash
cp .env.example .env     # Windows: copy .env.example .env
```

在 `.env` 中设置:

```bash
# 默认厂商 DeepSeek
DEEPSEEK_API_KEY=sk-你的key
```

若改用其它 OpenAI 兼容厂商,在 `config.yaml` 改 `provider.base_url`,并设置对应环境变量(`OPENAI_API_KEY` / `MOONSHOT_API_KEY` / `DASHSCOPE_API_KEY` / `ZHIPUAI_API_KEY` 等)。

`.env` 已被 `.gitignore` 忽略,不会提交入库。

**key 在哪里读取(按优先级)**:进程环境变量 > `<工作区>/.env` > `~/.coding_agent/.env`。

- **只在本仓库内用**:在仓库根 `.env` 配置即可(如上)。
- **想在任意目录启动、处理其它项目**(推荐):把 key 放到**用户级 `~/.coding_agent/.env`**,任何工作区启动都会读它:

  ```
  # Windows:C:\Users\<你>\.coding_agent\.env
  DEEPSEEK_API_KEY=sk-你的key
  ```

  或直接把 `DEEPSEEK_API_KEY` 设为**系统环境变量**(优先级最高,但需手动在系统设置里配置)。

> 提示:同一项目目录若同时存在 `<工作区>/.env` 与 `~/.coding_agent/.env`,前者优先;两者都不存在时启动会提示未配置 key。

## 二、启动方式

在仓库根目录下,激活 conda 环境后执行:

### 1. 交互式终端(推荐,类 Claude Code)

```bash
python -m codingagent
```

流式输出 + 多轮对话 + 工具调用面板;`/help` 查看命令;Ctrl+C 中断当前回合。

### 2. 一次性模式(脚本/演示)

```bash
python -m codingagent run "读取 README.md,并在末尾追加一行说明"
```

非交互,直接输出最终结果,适合录制演示视频。

### 3. 网页端(对话管理 + 流式)

```bash
python -m codingagent web            # 默认 http://127.0.0.1:8787
python -m codingagent web --port 9000
```

浏览器打开地址:左侧会话列表,右侧聊天窗,流式渲染模型输出与工具调用过程;支持斜杠命令。生成中可点「停止」按钮立即中断(前端中止流式连接,后端同时置位中断信号,工具循环/子 Agent 团队一并退出)。工具栏「主题」下拉可即时切换整体配色(深色 / 浅色 / 暖色 / 夜间蓝 / 南大紫 / 软件蓝),选择记忆在浏览器本地(下次打开自动恢复)。代码块右上角带「复制」按钮,一键复制整段代码;侧栏顶部有会话标题搜索框,输入即过滤;会话首次提问后自动以首条消息命名标题(先由启发式提取核心内容即时命名,再用模型精修为语义化标题,如「帮我写一个快速排序算法并解释」→「快速排序算法实现与解释」),双击会话标题可直接重命名;每条用户消息悬停出现「✎」按钮,点击后把该条文本回填输入框,**从该条起重新生成**(其后的旧回复被丢弃,可反复编辑重发)。

**输入框交互**:
- **斜杠命令自动补全**:在输入框敲 `/` 弹出命令面板(内置 + 自定义斜杠命令),方向键上下选择、Enter / Tab 补全、Esc 关闭,点选亦可。
- **权限审批按钮**:在「交互」权限模式下,agent 遇到需要确认的操作(如写文件、越沙箱路径、危险命令、WebSearch)时,输入框上方弹出「允许 / 拒绝」按钮,点选后即时放行或拦截,不再像早期版本那样默认拒绝。
- **选项选择(ask_user)**:当任务存在歧义、有多个可行方向、需要用户拍板时,agent 会调用 `ask_user` 工具向用户提问并给出 2-6 个选项,输入框上方弹出选项条——直接点选、点「其他 / 自定义」输入文本,或点「取消」;选择结果会回灌给 agent,它按你的选择继续执行。CLI 下同样触发:终端打印编号列表(含「其他」),输入编号或自定义文本即可。

**导出会话**:工具栏「导出会话」下拉把当前会话下载为文件,共三种格式:

| 格式 | 内容 | 用途 |
|---|---|---|
| Markdown(`.md`) | 可读对话转写:标题 + 时间 + 分节的用户/助手消息、工具调用与结果(工具结果名自动回填) | 用任意 Markdown 编辑器/笔记打开即可渲染,适合写报告、README、教学演示 |
| JSON(`.json`) | 与 `.coding_agent/sessions/*.json` 同构的原始历史(消息 + 用量 + 摘要) | 备份会话、迁移,或在本页「导入会话」恢复、程序化再处理 |
| 纯文本(`.txt`) | 无格式转写(【用户】/【助手】/工具记录) | 直接粘贴到聊天、邮件等纯文本场景 |

下载文件名即会话标题(非法字符替换为 `_`);导出内容为该会话**已保存**的历史,未完成/进行中的回合不包含。

**导入会话**:网页端工具栏点「导入会话」,选择之前导出的 JSON 文件(或任意与 `{"messages": [...]}` 同构的历史文件),即以**新会话**恢复——不会覆盖现有会话;导入后消息立即展示,可直接继续对话或再次导出。

### 4. 环境自检

```bash
python -m codingagent doctor
```

输出工作区、模型、工具、技能、斜杠命令、权限模式、MCP 连接状态,便于排查环境问题。

> **处理其它目录的项目**:先执行 `pip install -e .` 把包装进环境(见"环境准备"),之后 `cd` 进目标项目再启动即可——
> 工作区自动按"当前目录向上找最近 `.git`"决定;或直接用 `-w` 指定:`python -m codingagent -w D:\目标项目`(交互 / run / web / doctor 均支持)。
> 会话内的工作区在启动时固定,中途不可切换。
> **注意**:换目录后 API key 不会自动带上——需把 key 放在用户级 `~/.coding_agent/.env` 或系统环境变量(见"一、配置 API key")。

## 三、配置说明(`config.yaml`)

| 段 | 关键项 | 说明 |
|---|---|---|
| `provider` | `base_url` / `model` / `temperature` | 模型厂商与参数,任意 OpenAI 兼容网关可换 |
| `context` | `budget_tokens` / `max_tool_output` | 上下文预算(超限自动压缩)、单条工具输出上限 |
| `agent` | `max_iterations` | 单任务最大工具迭代轮数 |
| `permissions` | `mode` / `sandbox` / 名单 | 权限策略(见下) |
| `memory` | `enabled` / 文件路径 | 跨会话记忆开关 |
| `search` | `base_url` / `timeout` / `max_results` / `proxy` | 联网搜索(WebSearch)参数,无需 key,默认抓取 Bing |
| `mcp` | `servers` | 外部工具服务(见下) |
| `teams` | `成员定义` | 多 Agent 团队(见下) |
| `hooks` | 事件 → 命令/可调用 | 生命周期钩子(见下) |

## 四、权限模式

| mode | 行为 |
|---|---|
| `interactive`(默认) | 未知操作逐次询问用户(CLI 输入 y/n;Web 端在输入框上方弹「允许/拒绝」按钮) |
| `auto-approve` | 白名单外自动放行(需用户明确开启) |
| `deny` | 白名单外一律拒绝 |

纵深防御:敏感路径(`.env`、密钥、`.git`)直接拒绝;越出工作区沙箱需确认;`rm -rf`、`git push --force` 等危险命令需确认。可在 `config.yaml` 配置 `allow_tools` / `deny_tools` / `ask_tools` / `allow_commands` / `dangerous_commands`。

> **联网搜索默认需确认**:`ask_tools` 默认含 `WebSearch`,即每次联网抓取外部内容都会先征求用户同意;若希望自动放行,把它移入 `allow_tools`。
>
> **ask_user 豁免**:`ask_user`(向用户提问并给出选项)是纯用户交互工具,任何权限模式下都直接弹出选项,不会被权限确认拦截——除非显式把它加进 `deny_tools`。

## 五、斜杠命令清单

| 命令 | 说明 |
|---|---|
| `/help` | 显示帮助 |
| `/status` | 显示会话状态(工作区/模型/迭代/预算/用量) |
| `/exit` (`/quit`) | 退出 |
| `/clear` | 清空当前会话上下文 |
| `/model <名>` | 查看/切换模型 |
| `/tools` | 列出可用工具 |
| `/permissions` | 显示权限策略 |
| `/memory [project\|user\|save <scope> <条目>\|clear]` | 查看/保存跨会话记忆 |
| `/skills [list\|load <名>\|unload <名>]` | 管理技能包 |
| `/compact` | 手动压缩上下文 |
| `/cost` | 查看 token 用量与估算费用 |
| `/mcp [list\|connect <名>\|disconnect <名>]` | 管理 MCP servers |
| `/team <团队名> <任务>` | 运行 Agent 团队 |
| `/worktree [list\|create [分支]\|remove <路径>]` | 管理 Git worktree |
| `/resume` | 恢复最近会话历史(CLI) |

**自定义命令**:在 `.coding_agent/commands/` 放 `*.md` 文件,首行 `# 名称 - 一句话说明`,其余为发给 Agent 的 prompt 模板,`{args}` 会被替换成命令参数。示例见 `examples/commands/plan.md`(先规划再执行)。

## 六、MCP(外部工具服务)

在 `config.yaml` 配置 `mcp.servers`,启动时自动连接:

```yaml
mcp:
  servers:
    demo:
      command: python
      args: [examples/mcp_server_demo.py]
      cwd: "."
```

内置演示 server 提供 `demo_echo` / `demo_add` 两个工具,用于验证链路。任何符合 MCP 规范的本地服务(GitHub、数据库等)均可按同样方式挂载;`/mcp` 命令管理连接。

## 七、技能(Skill)

在 `skills/`、`.coding_agent/skills`、`~/.coding_agent/skills` 任一处放技能包目录:

```
技能名/
├── SKILL.md       # frontmatter(name/description)+ 正文 instructions
├── tools/*.py     # 可选:额外工具(暴露 TOOLS 列表)
└── resources/**   # 可选:静态资源
```

`/skills list` 查看,`/skills load <名>` 装载(instructions 注入 system、工具注册)。内置示例:
- `skills/code-review` — 代码审查,产出结构化审查报告;
- `skills/tsp` — TSP(团队软件过程):分阶段推进、数据驱动的计划与质量跟踪;
- `skills/scrum` — Scrum(敏捷迭代):用户故事 + 冲刺 + 完成定义(DoD)。

> 新增技能包后需**重启会话**才会被扫描到(扫描发生在启动时)。

## 八、Agent 团队 / Worktree / 钩子

- **团队**:`config.yaml` 的 `teams` 定义成员(名称/角色/模型/工具白名单/提示),`/team <团队名> <任务>` 触发:成员并行作业,负责人汇总。内置示例:
  - `review-team` — 架构师 / 工程师 / 审查员(代码审查与实现);
  - `tsp-team` — TSP 六角色(负责人 / 规划经理 / 设计经理 / 实现经理 / 质量经理 / 测试经理);
  - `scrum-team` — 产品负责人 / Scrum教练 / 开发成员 / 测试成员(一个冲刺)。
  用法:`/team tsp-team 用 TSP 方式为该项目完成一次计划+设计`。内置团队的成员默认**只输出分析与结论文本、不各自写文件**(避免多成员并行散落多份重复文档);负责人汇总成功后把最终成果**统一写入工作区 `<团队名>_汇总.md`**,聊天里也会给出内容与文件路径。自定义团队若保留成员的 `WriteFile` 权限,多成员同时写同一文件仍可能冲突,需隔离并行时配合 `/worktree`。
- **Worktree**:`/worktree create [分支]` 创建独立工作树,`/worktree remove <路径>` 清理,适合多 Agent 并行隔离。
- **钩子**:`config.yaml` 的 `hooks` 配置事件(如 `pre_tool_call`),值为 shell 命令或 `"模块.函数"` 可调用对象;`pre_tool_call` 返回失败可拦截该次工具调用。

## 九、测试

```bash
python -m pytest tests/ -q
```

66 个用例全部用 mock LLM,无需真实 API key,覆盖配置/工具/权限/循环/扩展/Web API 等核心逻辑。

## 十、常见问题(FAQ)

| 现象 | 原因与解决 |
|---|---|
| 提示"未检测到 API key" | 未配置 `.env`/环境变量,见上文"配置 API key";若换了项目目录,需把 key 放到 `~/.coding_agent/.env`(用户级)或设系统环境变量;运行 `python -m codingagent doctor` 检查 |
| 鉴权失败(401/403) | key 无效或已过期;更换后重试 |
| 中文/emoji 乱码 | Windows 控制台编码问题,程序已自动切 UTF-8;若仍异常请在终端执行 `chcp 65001` |
| Web 打不开 | 确认已安装 fastapi/uvicorn(`pip install -r requirements.txt`),且端口未被占用 |
| 命令执行被拦截 | 命中危险命令名单或越出沙箱,属正常权限保护;如确需放行请调整 `config.yaml` 权限配置 |
| MCP 连接失败 | 确认 server 命令可执行、路径正确;`/mcp list` 查看状态 |
| WebSearch 搜索为空/失败 | 网络不可达或 Bing 拦截;可把 `search.base_url` 换成 `https://cn.bing.com/search`,或配置 `search.proxy` 走代理后重试 |
| 换目录后 `python -m codingagent` 找不到模块 | 未把包装进环境:在仓库根执行 `pip install -e .` 一次;之后任何目录都可启动,并用 `-w` 指定工作区 |
